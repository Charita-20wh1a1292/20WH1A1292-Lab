#include<stdio.h>
#include<math.h>
int main()
{
	int num,power,i,sum=0;
	printf("Enter number:\n");
	scanf("%d",&num);
	printf("Enter power:\n");
	scanf("%d",&power);
	for(i=0;i<=power;i++)
	{
		sum=sum+pow(num,i);
		printf("%d^%d+ ",num,i);
	}
	printf("\nsum=%d",sum);
}
